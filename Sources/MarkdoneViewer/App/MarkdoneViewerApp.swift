import MarkdoneViewerCore
import SwiftUI

@main
struct MarkdoneViewerApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Untitled")
                .font(.system(.body))
                .frame(minWidth: 800, minHeight: 600)
        }
    }
}
